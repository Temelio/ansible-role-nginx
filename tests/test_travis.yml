---

# Role travis tests

- hosts: localhost
  remote_user: root
  roles:
    - "{{ role_name }}"
  post_tasks:
    - include: ./test_common.yml
  vars:
    nginx_ssl_self_signed_cn: "foobar.bar"
    role_name: "{{ lookup('env','TRAVIS_REPO_SLUG') }}"

